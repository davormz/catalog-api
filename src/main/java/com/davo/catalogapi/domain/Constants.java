package com.davo.catalogapi.domain;

public class Constants {
    
    //Enumeration called StockOperationType with the following values: IN, OUT
    public enum StockOperationType {
        IN, OUT
    }
}
